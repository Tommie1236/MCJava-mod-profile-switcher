/*const fileInput = document.getElementById('file-input');
fileInput.addEventListener('change', (event) => {
	const file = event.target.files; // uploaded file
})*/

function selectVersion() {
	console.log('version');
};
function selectModloader() {
	console.log('modloader');
};